# React Native / Expo work most reliably with hoisted node_modules
node-linker=hoisted

# Expo / RN often reach into packages that aren't direct deps
shamefully-hoist=true

# Keep the install deterministic across machines
strict-peer-dependencies=false
auto-install-peers=true
